{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug (gdb, integrado)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}/${fileBasenameNoExtension}.exe",
            "cwd": "${fileDirname}",
            "MIMode": "gdb",
            "miDebuggerPath": "C:/msys64/ucrt64/bin/gdb.exe",
            "externalConsole": false,
            "preLaunchTask": "C/C++: g++.exe build active file"
        }
    ]
}